import Root, { type ButtonProps, type ButtonSize, buttonVariants } from './button.svelte';

export type { ButtonVariant } from './button.svelte';
export {
  type ButtonProps as Props,
  type ButtonProps,
  type ButtonSize,
  buttonVariants,
  Root,
  //
  Root as Button
};
